# NetCreate ignored directories
/runtime

# Also ignore build system directories
.npmignore
/public
/node_modules

# And don't commit personal log files
*_log.md

# And don't commit .zip archives either
*.zip
